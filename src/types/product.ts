export interface ProductData {
  id: number;
  product_name: string;
  contractor: string;
}

export interface FormData {
  motorId: string;
  employeeId: string;
  workDate: string;
  workTime: string;
  bearingId: string;
  fanId: string;
  sealId: string;
}
